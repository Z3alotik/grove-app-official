import { makeStyles } from "@mui/styles";

const useStyles = makeStyles(() => ({
  eventInfoText: {
    color: "white",
    fontFamily: "Teko",
    fontSize: "1.5rem",
    lineHeight: "0.5rem",
  },
  wrapperGrid: {
    width: "100%",
    overflow: "auto",
  },
}));

export default useStyles;
